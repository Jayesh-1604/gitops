apiVersion: v1
kind: Service
metadata:
  name: pingpongapp-service
  labels:
    app: pingpongapp
spec:
  type: ClusterIP
  selector:
    app: pingpongapp
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
